// 노노그램 숫자 입력 → 이미지(테이블)로 변환 (기본 구조)
document.getElementById('generate-btn').addEventListener('click', function() {
    const rowInput = document.getElementById('row-input').value.trim();
    const colInput = document.getElementById('col-input').value.trim();
    // TODO: 숫자 파싱 및 테이블 생성 로직 구현 예정
    document.getElementById('nonogram-output').innerHTML = '<p>아직 구현 전입니다.</p>';
});
